interface:
  display_name: "LunaMaxing"
  short_description: "Configurable specialist routing with eager delegation"
  default_prompt: "Use $lunamaxing to decompose this non-trivial task into specialist lanes, apply project model routing, and verify every result."
